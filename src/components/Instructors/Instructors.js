import React from 'react';
import Navbar from '../Navbar/Navbar';

const Instructors = () => {
  return (
    <div>
        <div className="column">
          </div>
    <p>This is the instructors page! {Math.random()*100}</p>
    </div>
  );
};

export default Instructors;
