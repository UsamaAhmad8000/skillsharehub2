import React from 'react';
import Navbar from '../Navbar/Navbar';

const Courses = () => {
  return (
    <div>
        <div className="column">
            
          </div>
    <p>This is the courses page! {Math.random()*100}</p>
    </div>
  );
};

export default Courses;
